## 2025-02-23 - Icon Consistency
**Learning:** Checking `package.json` for existing icon libraries (like FontAwesome) prevents build errors and bloat compared to assuming `react-icons` availability.
**Action:** Always `grep` for icon usage before importing new libraries.
